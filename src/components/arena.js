import React, {Component} from 'react';

export class Arena extends Component {
    render() {
        const {arena} = this.props;
        console.log(arena)
        return (
            <div>
                
            </div>
        );
    }
}